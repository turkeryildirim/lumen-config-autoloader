{
    "name": "turkeryildirim/lumen-config-autoloader",
    "description": "This package provides an easy way to autoload config files from your application's config folder.",
    "authors": [
        {
            "name": "Türker YILDIRIM",
            "email": "me@turkeryildirim.com",
            "homepage": "http://turkeryildirim.com"
        }
    ],
    "homepage": "https://github.com/turkeryildirim/lumen-config-autoloader",
    "type": "library",
    "keywords": ["lumen", "config", "autoload", "api", "load"],
    "license": "MIT",
    "minimum-stability": "stable",
    "require": {
        "php": ">=5.6",
        "laravel/lumen-framework": "^5.4"
    },
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "Turker\\ConfigAutoloader\\": "src/"
        }
    },
    "scripts": {
        "check-style": "phpcs -p --standard=PSR2 src/",
        "fix-style": "phpcbf -p --standard=PSR2 src/"
    },
    "prefer-stable": true
}
